# 003 macOS Builder

Public build repository for 003 macOS test packages.

Only encrypted build input and automation files are stored here; plain source code and release credentials are not included.
